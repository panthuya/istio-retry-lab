apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: backend-vs
spec:
  hosts:
  - backend
  http:
  - route:
    - destination:
        host: backend
        port:
          number: 6565
    fault:
      abort:
        httpStatus: 503
        percentage:
          value: 100
    retries:
      attempts: 3
      perTryTimeout: 2s
      retryOn: cancelled,unavailable,resource-exhausted
